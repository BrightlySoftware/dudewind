{
    "private": true,
    "scripts": {
        "build": "cross-env NODE_ENV=development node_modules/webpack/bin/webpack.js --progress --hide-modules --config=webpack.config.js && npm run scss",
        "scss": "mkdir -p dist && cp -R src/scss dist/scss"
    },
    "devDependencies": {
        "cross-env": "^5.1",
        "css-loader": "^0.28.7",
        "extract-text-webpack-plugin": "^3.0.2",
        "postcss": "^6.0.14",
        "postcss-loader": "^2.0.8",
        "style-loader": "^0.19.0",
        "tailwindcss": "^0.5.3",
        "webpack": "^3.8.1"
    }
}
